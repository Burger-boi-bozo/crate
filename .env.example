# Where completed downloads are stored on the Docker host.
DOWNLOAD_PATH=./downloads

# Number of jobs allowed to run at once.
UDM_MAX_CONCURRENT=2

# Strongly recommended when exposing the app through a tunnel or reverse proxy.
UDM_USERNAME=daniel
UDM_PASSWORD=change-this-to-a-long-random-password

TZ=America/Chicago

